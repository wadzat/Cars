{% extends 'base.html.twig' %}

{% block title %}{{ model.brand.name ~ model.name }}!{% endblock %}

{% block body %}
    <div>
        <h1><a href="{{ path('brand_show', {slug: model.brand.slug}) }}">{{ model.brand }}</a>  {{ model.name }}</h1>

        <img src="{{ asset('/upload/images/brand/' ~ model.brand.logo) }}" />

    </div>
{% endblock %}
